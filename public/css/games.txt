/* ================================================
   GAME STYLES - COMPREHENSIVE GAME UI
   ================================================ */

:root {
  --color-common: #6B7280;
  --color-uncommon: #10B981;
  --color-rare: #3B82F6;
  --color-epic: #A855F7;
  --color-legendary: #F59E0B;
  --color-primary: #8b5cf6;
  --color-secondary: #ec4899;
  --shadow-lg: 0 20px 25px -5px rgba(0, 0, 0, 0.3);
}

/* ================================================
   GENERAL GAME CONTAINER
   ================================================ */

.game-container {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
  background: linear-gradient(135deg, #0f0720 0%, #1a0b2e 100%);
  border-radius: 20px;
  color: white;
}

.game-header {
  text-align: center;
  margin-bottom: 40px;
  animation: slideDown 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.game-header h1 {
  font-size: 2.5rem;
  font-weight: 900;
  background: linear-gradient(135deg, #8b5cf6, #ec4899);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin: 0;
}

.game-header p {
  color: #d1d5db;
  margin: 10px 0 0;
  font-size: 1.1rem;
}

.game-info {
  display: flex;
  justify-content: center;
  gap: 30px;
  flex-wrap: wrap;
  margin-top: 20px;
}

.info-badge {
  background: rgba(139, 92, 246, 0.2);
  border: 1px solid #8b5cf6;
  padding: 10px 20px;
  border-radius: 20px;
  font-size: 0.9rem;
}

.info-badge strong {
  color: #8b5cf6;
}

/* ================================================
   BOXES GAME
   ================================================ */

.boxes-game {
  display: grid;
  gap: 20px;
  margin-bottom: 40px;
}

.boxes-grid {
  display: grid;
  gap: var(--gap, 15px);
  grid-template-columns: repeat(var(--columns, 3), 1fr);
  margin: 30px 0;
  animation: fadeIn 0.8s ease-out;
}

.box {
  aspect-ratio: 1;
  background: linear-gradient(135deg, #1e1b4b, #3b2667);
  border: 2px solid #8b5cf6;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  font-size: 3rem;
  font-weight: bold;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  box-shadow: 0 8px 20px rgba(139, 92, 246, 0.3);
  position: relative;
  overflow: hidden;
}

.box::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
  transition: left 0.5s ease;
}

.box:hover {
  transform: scale(1.05) translateY(-5px);
  border-color: #ec4899;
  box-shadow: 0 12px 30px rgba(139, 92, 246, 0.5);
}

.box:hover::before {
  left: 100%;
}

.box.selected {
  background: linear-gradient(135deg, #8b5cf6, #ec4899);
  animation: pulse 0.6s ease-out;
}

.box.revealed {
  background: var(--rarity-color, #6B7280);
  border-color: var(--rarity-color, #6B7280);
  cursor: default;
  pointer-events: none;
}

.box-content {
  text-align: center;
  z-index: 1;
}

.box-number {
  display: block;
  font-size: 2rem;
  margin-bottom: 5px;
}

.box-prize {
  display: none;
  font-size: 0.9rem;
  color: #fff;
  word-break: break-word;
}

.box.revealed .box-number {
  display: none;
}

.box.revealed .box-prize {
  display: block;
}

/* ================================================
   WHEEL GAME
   ================================================ */

.wheel-container {
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 40px 0;
  min-height: 500px;
  perspective: 1000px;
}

.wheel-wrapper {
  position: relative;
  width: 400px;
  height: 400px;
  display: flex;
  justify-content: center;
  align-items: center;
}

.wheel {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  display: flex;
  justify-content: center;
  align-items: center;
  position: relative;
  box-shadow: var(--shadow-lg), inset 0 0 30px rgba(0,0,0,0.5);
  transition: transform 0.1s linear;
}

.wheel.spinning {
  animation: spinWheel 3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.wheel-segment {
  position: absolute;
  width: 100%;
  height: 100%;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
  color: white;
  font-size: 0.9rem;
  clip-path: polygon(50% 50%, 50% 0%, 100% 0%);
}

.wheel-pointer {
  position: absolute;
  top: -20px;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 15px solid transparent;
  border-right: 15px solid transparent;
  border-top: 25px solid #F59E0B;
  z-index: 10;
  filter: drop-shadow(0 2px 4px rgba(0,0,0,0.3));
}

.wheel-center {
  position: absolute;
  width: 60px;
  height: 60px;
  background: radial-gradient(circle, #ec4899, #8b5cf6);
  border-radius: 50%;
  z-index: 5;
  box-shadow: 0 8px 20px rgba(0,0,0,0.4);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.5rem;
}

.spin-button {
  background: linear-gradient(135deg, #8b5cf6, #ec4899);
  color: white;
  padding: 15px 40px;
  border: none;
  border-radius: 50px;
  font-size: 1.1rem;
  font-weight: bold;
  cursor: pointer;
  box-shadow: var(--shadow-lg);
  transition: all 0.3s ease;
  margin-top: 30px;
}

.spin-button:hover {
  transform: scale(1.05);
  box-shadow: 0 25px 35px rgba(139, 92, 246, 0.4);
}

.spin-button:active {
  transform: scale(0.95);
}

.spin-button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

@keyframes spinWheel {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(3600deg);
  }
}

/* ================================================
   SCRATCH GAME
   ================================================ */

.scratch-container {
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 40px 0;
}

.scratch-card {
  position: relative;
  width: 400px;
  height: 500px;
  border-radius: 15px;
  overflow: hidden;
  box-shadow: var(--shadow-lg);
  background: white;
}

.scratch-revealed {
  width: 100%;
  height: 100%;
  padding: 40px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  background: linear-gradient(135deg, #1e1b4b, #3b2667);
  color: white;
}

.scratch-revealed .prize-emoji {
  font-size: 4rem;
  margin-bottom: 20px;
}

.scratch-revealed .prize-name {
  font-size: 1.5rem;
  font-weight: bold;
  color: #8b5cf6;
  margin-bottom: 10px;
}

.scratch-revealed .prize-value {
  font-size: 1rem;
  color: #d1d5db;
}

.scratch-canvas {
  position: absolute;
  top: 0;
  left: 0;
  cursor: crosshair;
  background: linear-gradient(135deg, #d4af37, #f4e4c1);
  font-size: 1.2rem;
  text-align: center;
  display: flex;
  align-items: center;
  justify-content: center;
}

.scratch-text {
  font-weight: bold;
  color: #333;
  user-select: none;
}

.scratch-info {
  text-align: center;
  margin-top: 20px;
  color: #d1d5db;
}

.scratch-progress {
  width: 100%;
  height: 4px;
  background: rgba(139, 92, 246, 0.2);
  border-radius: 2px;
  margin-top: 10px;
  overflow: hidden;
}

.scratch-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #8b5cf6, #ec4899);
  width: 0%;
  transition: width 0.3s ease;
}

/* ================================================
   MYSTERY GAME
   ================================================ */

.mystery-game {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 30px;
  margin: 40px 0;
}

.mystery-boxes {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
  gap: 15px;
  margin: 30px 0;
  max-width: 600px;
}

.mystery-box {
  aspect-ratio: 1;
  background: linear-gradient(135deg, #1e1b4b, #3b2667);
  border: 2px solid #8b5cf6;
  border-radius: 10px;
  padding: 15px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  box-shadow: 0 8px 20px rgba(139, 92, 246, 0.3);
  position: relative;
}

.mystery-box:hover {
  transform: scale(1.05) rotateY(5deg);
  border-color: #ec4899;
  box-shadow: 0 12px 30px rgba(139, 92, 246, 0.5);
}

.mystery-box-number {
  font-size: 2rem;
  font-weight: bold;
  color: #8b5cf6;
  transition: opacity 0.3s;
}

.mystery-box.found {
  opacity: 0.5;
  border-color: #10B981;
  cursor: default;
}

.mystery-box.found .mystery-box-number {
  display: none;
}

.mystery-box-prize {
  color: #10B981;
  font-size: 0.8rem;
  font-weight: bold;
  display: none;
}

.mystery-box.found .mystery-box-prize {
  display: block;
}

.mystery-stats {
  display: flex;
  gap: 20px;
  justify-content: center;
  flex-wrap: wrap;
}

.stat-item {
  background: rgba(139, 92, 246, 0.2);
  border: 1px solid #8b5cf6;
  padding: 15px 30px;
  border-radius: 10px;
  text-align: center;
}

.stat-label {
  color: #d1d5db;
  font-size: 0.9rem;
  margin-bottom: 5px;
}

.stat-value {
  color: #8b5cf6;
  font-size: 1.5rem;
  font-weight: bold;
}

/* ================================================
   GACHA GAME
   ================================================ */

.gacha-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 30px;
  margin: 40px 0;
}

.pull-buttons {
  display: flex;
  gap: 15px;
  flex-wrap: wrap;
  justify-content: center;
}

.pull-button {
  background: linear-gradient(135deg, #8b5cf6, #ec4899);
  color: white;
  border: none;
  padding: 12px 30px;
  border-radius: 25px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(139, 92, 246, 0.3);
}

.pull-button:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 25px rgba(139, 92, 246, 0.5);
}

.pull-button.bulk {
  background: linear-gradient(135deg, #10B981, #059669);
}

.pulls-result {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(100px, 1fr));
  gap: 15px;
  width: 100%;
  max-width: 600px;
}

.pull-item {
  aspect-ratio: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border-radius: 10px;
  padding: 20px;
  text-align: center;
  animation: popIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
  cursor: default;
}

.pull-item.common { background: linear-gradient(135deg, #6B7280, #4b5563); }
.pull-item.uncommon { background: linear-gradient(135deg, #10B981, #059669); }
.pull-item.rare { background: linear-gradient(135deg, #3B82F6, #1d4ed8); }
.pull-item.epic { background: linear-gradient(135deg, #A855F7, #7c3aed); }
.pull-item.legendary { background: linear-gradient(135deg, #F59E0B, #d97706); }

.pull-item-emoji {
  font-size: 2.5rem;
  margin-bottom: 10px;
}

.pull-item-name {
  font-weight: bold;
  color: white;
  font-size: 0.9rem;
  margin-bottom: 5px;
}

.pull-item-rarity {
  font-size: 0.8rem;
  opacity: 0.9;
}

.pity-status {
  background: rgba(139, 92, 246, 0.2);
  border: 1px solid #8b5cf6;
  padding: 15px 30px;
  border-radius: 10px;
  text-align: center;
}

.pity-label {
  color: #d1d5db;
  font-size: 0.9rem;
}

.pity-progress {
  width: 200px;
  height: 8px;
  background: rgba(139, 92, 246, 0.3);
  border-radius: 4px;
  margin: 10px auto;
  overflow: hidden;
}

.pity-bar {
  height: 100%;
  background: linear-gradient(90deg, #8b5cf6, #F59E0B);
  width: 0%;
  transition: width 0.3s ease;
}

.pity-count {
  color: #F59E0B;
  font-weight: bold;
  font-size: 1.2rem;
}

/* ================================================
   DICE GAME
   ================================================ */

.dice-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 30px;
  margin: 40px 0;
}

.dice-roller {
  display: flex;
  gap: 20px;
  justify-content: center;
  margin: 30px 0;
}

.dice {
  width: 80px;
  height: 80px;
  background: linear-gradient(135deg, #1e1b4b, #3b2667);
  border: 2px solid #8b5cf6;
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 2.5rem;
  font-weight: bold;
  color: white;
  box-shadow: 0 8px 20px rgba(139, 92, 246, 0.3);
  transition: all 0.1s ease;
}

.dice.rolling {
  animation: rollDice 0.1s linear infinite;
}

.roll-button {
  background: linear-gradient(135deg, #8b5cf6, #ec4899);
  color: white;
  border: none;
  padding: 15px 40px;
  border-radius: 50px;
  font-size: 1.1rem;
  font-weight: bold;
  cursor: pointer;
  box-shadow: var(--shadow-lg);
  transition: all 0.3s ease;
}

.roll-button:hover {
  transform: scale(1.05);
  box-shadow: 0 25px 35px rgba(139, 92, 246, 0.4);
}

.roll-button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.roll-result {
  text-align: center;
  font-size: 1.2rem;
  color: #d1d5db;
  margin-top: 20px;
}

.roll-result strong {
  color: #8b5cf6;
  font-size: 1.5rem;
}

@keyframes rollDice {
  0%, 100% { transform: rotateX(0deg) rotateY(0deg); }
  25% { transform: rotateX(20deg) rotateY(-20deg); }
  50% { transform: rotateX(-20deg) rotateY(20deg); }
  75% { transform: rotateX(20deg) rotateY(20deg); }
}

/* ================================================
   CARDS GAME
   ================================================ */

.cards-game {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 30px;
  margin: 40px 0;
}

.cards-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(100px, 1fr));
  gap: 12px;
  max-width: 500px;
}

.card {
  aspect-ratio: 1;
  background: linear-gradient(135deg, #1e1b4b, #3b2667);
  border: 2px solid #8b5cf6;
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  font-size: 2rem;
  font-weight: bold;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  box-shadow: 0 8px 20px rgba(139, 92, 246, 0.3);
  position: relative;
  transform-style: preserve-3d;
}

.card:hover {
  transform: translateY(-5px);
  box-shadow: 0 12px 30px rgba(139, 92, 246, 0.5);
}

.card.flipped {
  animation: flipCard 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.card-content {
  position: relative;
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.card-back {
  color: #8b5cf6;
}

.card-front {
  display: none;
  color: white;
  z-index: 2;
}

.card.flipped .card-back {
  display: none;
}

.card.flipped .card-front {
  display: flex;
}

.cards-stats {
  display: flex;
  gap: 20px;
  justify-content: center;
  flex-wrap: wrap;
}

@keyframes flipCard {
  0% {
    transform: rotateY(0deg) scale(1);
  }
  50% {
    transform: rotateY(90deg) scale(1.1);
  }
  100% {
    transform: rotateY(0deg) scale(1);
  }
}

/* ================================================
   ANIMATIONS
   ================================================ */

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes pulse {
  0%, 100% {
    transform: scale(1);
  }
  50% {
    transform: scale(1.05);
  }
}

@keyframes popIn {
  0% {
    opacity: 0;
    transform: scale(0.5);
  }
  100% {
    opacity: 1;
    transform: scale(1);
  }
}

/* ================================================
   RESULT SCREEN
   ================================================ */

.result-screen {
  background: linear-gradient(135deg, #0f0720, #1a0b2e);
  border-radius: 20px;
  padding: 40px;
  text-align: center;
  animation: slideDown 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.result-emoji {
  font-size: 4rem;
  margin-bottom: 20px;
}

.result-title {
  font-size: 2rem;
  font-weight: bold;
  background: linear-gradient(135deg, #8b5cf6, #ec4899);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 10px;
}

.result-message {
  color: #d1d5db;
  font-size: 1.2rem;
  margin-bottom: 20px;
}

.result-detail {
  background: rgba(139, 92, 246, 0.2);
  border: 1px solid #8b5cf6;
  padding: 20px;
  border-radius: 10px;
  margin: 20px 0;
  color: #d1d5db;
}

.result-buttons {
  display: flex;
  gap: 15px;
  justify-content: center;
  margin-top: 30px;
  flex-wrap: wrap;
}

.btn {
  padding: 12px 30px;
  border: none;
  border-radius: 25px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
}

.btn-primary {
  background: linear-gradient(135deg, #8b5cf6, #ec4899);
  color: white;
  box-shadow: var(--shadow-lg);
}

.btn-primary:hover {
  transform: translateY(-3px);
  box-shadow: 0 25px 35px rgba(139, 92, 246, 0.4);
}

.btn-secondary {
  background: transparent;
  border: 2px solid #8b5cf6;
  color: white;
}

.btn-secondary:hover {
  background: rgba(139, 92, 246, 0.2);
}

/* ================================================
   RESPONSIVE DESIGN
   ================================================ */

@media (max-width: 768px) {
  .game-container {
    padding: 15px;
  }

  .game-header h1 {
    font-size: 1.8rem;
  }

  .boxes-grid {
    grid-template-columns: repeat(auto-fit, minmax(60px, 1fr));
    gap: 10px;
  }

  .box {
    font-size: 2rem;
  }

  .wheel-wrapper {
    width: 300px;
    height: 300px;
  }

  .scratch-card {
    width: 300px;
    height: 400px;
  }

  .mystery-boxes {
    grid-template-columns: repeat(3, 1fr);
  }

  .pulls-result {
    grid-template-columns: repeat(3, 1fr);
  }

  .dice {
    width: 60px;
    height: 60px;
    font-size: 2rem;
  }

  .cards-grid {
    grid-template-columns: repeat(4, 1fr);
  }

  .result-screen {
    padding: 30px 20px;
  }

  .result-title {
    font-size: 1.5rem;
  }

  .game-info {
    gap: 15px;
    flex-direction: column;
  }

  .info-badge {
    width: 100%;
  }
}

@media (max-width: 480px) {
  .game-header h1 {
    font-size: 1.5rem;
  }

  .boxes-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 8px;
  }

  .box {
    font-size: 1.5rem;
  }

  .mystery-boxes {
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
  }

  .mystery-box {
    padding: 10px;
  }

  .mystery-box-number {
    font-size: 1.5rem;
  }

  .wheel-wrapper {
    width: 250px;
    height: 250px;
  }

  .wheel-center {
    width: 45px;
    height: 45px;
    font-size: 1rem;
  }

  .wheel-pointer {
    border-left: 10px solid transparent;
    border-right: 10px solid transparent;
    border-top: 20px solid #F59E0B;
  }

  .pulls-result {
    grid-template-columns: repeat(2, 1fr);
  }

  .dice {
    width: 50px;
    height: 50px;
    font-size: 1.5rem;
  }

  .cards-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 8px;
  }

  .card {
    font-size: 1.5rem;
  }

  .pull-buttons {
    flex-direction: column;
  }

  .pull-button {
    width: 100%;
  }
}

/* ================================================
   ACCESSIBILITY
   ================================================ */

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* Dark mode support */
@media (prefers-color-scheme: dark) {
  .scratch-card {
    background: #1a1a2e;
  }

  .scratch-canvas {
    background: linear-gradient(135deg, #d4af37, #f4e4c1);
  }

  .scratch-text {
    color: #333;
  }
}

/* ================================================
   UTILITY CLASSES
   ================================================ */

.loading {
  opacity: 0.6;
  pointer-events: none;
}

.disabled {
  opacity: 0.5;
  cursor: not-allowed;
  pointer-events: none;
}

.text-center {
  text-align: center;
}

.mt-20 {
  margin-top: 20px;
}

.mb-20 {
  margin-bottom: 20px;
}

.center {
  display: flex;
  align-items: center;
  justify-content: center;
}

.hidden {
  display: none !important;
}
